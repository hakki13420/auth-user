<table>
    <thead>
        <td>N°</td>
        <td>id</td>
        <td>username</td>
        <td>options</td>
    </thead>
    <tbody>
        <% users.forEach((el, index)=>{ %>
            <tr>
                <td>
                    <%= index+1 %>
                </td>
                <td>
                    <%= el._id %>
                </td>
                <td>
                    <%= el.username %>
                </td>
                <td class="options">
                    <button>Edit</button>
                    <button data-id=<%=el._id %> class="delbtn">Del</button>
                </td>
            </tr>

            <% })%>
    </tbody>
</table>